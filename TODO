- add nameserver to domain creation
- auto create serial in domain creation

Apps
=====

dns:

cmdb:
	- store network address as integer
	- when creating a network, creates all ip address related to the network
	- allocates the first and the last ip address in a network as busy (network and broadcast address)
